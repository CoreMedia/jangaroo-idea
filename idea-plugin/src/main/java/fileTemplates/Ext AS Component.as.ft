package ${PACKAGE_NAME}#if (${PACKAGE_NAME} != "") #end{

import ext.Ext;

public class ${NAME} extends ${Super_class_name} {

  public function ${NAME}(config:${Config_class_name} = null) {
    super(${Config_class_name}(Ext.apply({
      
    }, config)));
  }
}
}
