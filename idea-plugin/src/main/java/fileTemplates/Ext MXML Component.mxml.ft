<?xml version="1.0" encoding="UTF-8"?>
<!--- 
 Created by ${USER} on ${DATE}.
-->
<${Superclass}
        xmlns:fx="http://ns.adobe.com/mxml/2009"
        xmlns:ext="exml:ext.config">
  <fx:Script><![CDATA[
#if (${XTYPE} && ${XTYPE} != "")
    public static const xtype:String = "${XTYPE}";

#end
    private var config:${NAME};

    public native function ${NAME}(config:${NAME} = null);
    ]]></fx:Script>

</${Superclass}>
